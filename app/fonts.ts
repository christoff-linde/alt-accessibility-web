import { Darker_Grotesque, Inter, Noto_Serif } from 'next/font/google';

export const darkGrotesque = Darker_Grotesque({
  subsets: ['latin-ext'],
  display: 'swap',
});

export const inter = Inter({
  subsets: ['latin-ext'],
  display: 'swap',
});

export const notoSerif = Noto_Serif({
  subsets: ['latin-ext'],
  display: 'swap',
});
